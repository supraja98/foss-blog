from sqlalchemy import (
    Column,
    Integer,
    Text,
)

from .meta import Base


class Count(Base):
    __tablename__ = 'Count'
    name = Column(Text, primary_key=True)
    count = Column(Integer)
    topic = Column(Text)



